import os,requests
print("=== ALL ENV VARS ===")
for k,v in sorted(os.environ.items()):
 if "API" in k or "FOOT" in k or "KEY" in k:
  print(k,"=",v[:20] if len(str(v))>20 else v)
print("====================")
